using sys { String; log; }
using string { Builder; }
using utils{ forRange; }

fn itos(int i) @String {
    Builder.putInt(i).putCh(0x0a).toStr()
}

forRange(1, 101, (i){
    log(i % 5 == 0
        ? (i % 3 == 0 ? "fizzbuzz\n" : "fizz\n")
        : (i % 3 == 0 ? "buzz\n" : itos(i)))
})
