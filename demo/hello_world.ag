fn sys_log(sys_String msg);

class Test {
  fx = 0;

  m(int a, int b, sys_String s, (int)void lambda) {
    a += 1;
    fx += a;
    lambda(b);
    sys_log(s);
  }
}

oi = +1;
t = Test;
w=&t;
x = 0;
t.m(12, 4, "Hello World", (i){
  x+=i;
})