using sys { String; Blob; }

// Low level unsafe API
fn sdlInit(int flags) int;
fn sdlQuit();

fn createWindow(String title, int x,int y, int w, int h, int flags) int;
fn destroyWindow(int windowId);

fn createRenderer(int windowId, int index, int flags) int;
fn destroyRenderer(int rendererId);

fn waitEvent(Blob event);
fn pollEvent(Blob event);
fn delay(int millisec);

fn setRendererDrawColor(int rendererId, int r, int g, int b, int a);
fn rendererClear(int rendererId);
fn rendererFillRect(int rendererId, int x, int y, int w, int h);
fn blt(int rendererId, int textureId, int sx, int sy, int sw, int sh, int dx, int dy, int dw, int dh);
fn rendererPresent(int rendererId);

fn createTextureFromSurface(int rendererId, int surfaceId) int;
fn destroyTexture(int textureId);
fn setTextureAlphaMod(int texId, int multiplier);
fn setTextureColorMod(int id, int color);
fn freeSurface(int surfaceId);

fn imgInit(int flags) int;
fn imgQuit();
fn imgLoad(String fileName) int; // returns surfaceId
